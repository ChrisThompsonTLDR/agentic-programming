# Ideas

Researched idea notes written by the **`/idea`** agent (`idea.agent.md`). Shape and optional frontmatter: `.steering/templates/idea.md`.

**Naming:** prefer `<slug>.md` (kebab-case). Use a date prefix only if you need disambiguation (`YYYY-MM-DD-<slug>.md`).
